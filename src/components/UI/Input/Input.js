import React from 'react'
import { Input as BaseInput } from 'grommet'

const Input = props => {
  return (
    <BaseInput {...props} />
  )
}

Input.propTypes = {}
Input.defaultProps = {}

export default Input
